import React from 'react';
import Form from './innerComp/form';

const SideBar = () => {
    return (
        <div className='SideBar'>
            <Form></Form>
        </div>
    );
}

export default SideBar;
